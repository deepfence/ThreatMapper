# The following env variable is for dev mode
# only. It is used by vite to proxy api requests
# on the localhost to the actual api server

#Testing
USERNAME=youremail
PASSWORD=yourpassword
VITE_DEV_TEST_API_BASE_URL=https://url.com
